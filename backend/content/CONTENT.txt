Default ingest indexes **only** `backend/content/quick-topics/` (same themes as the chat quick-access chips). Add or edit `.md`/`.txt`/`.pdf` there, then run `npm run ingest`. For the old university-wide crawl plus files in this folder’s parent `content/`, run `npm run ingest:all`.
